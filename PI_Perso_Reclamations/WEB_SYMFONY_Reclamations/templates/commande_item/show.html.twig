{% extends 'base.html.twig' %}

{% block title %}CommandeItem{% endblock %}

{% block body %}
    <h1>CommandeItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ commande_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_item_index') }}">back to list</a>

    <a href="{{ path('app_commande_item_edit', {'id': commande_item.id}) }}">edit</a>

    {{ include('commande_item/_delete_form.html.twig') }}
{% endblock %}
