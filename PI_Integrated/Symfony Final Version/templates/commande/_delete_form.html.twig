<form method="post" action="{{ path('app_commande_delete', {'idC': commande.idC}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idC) }}">
    <button class="btn">Delete</button>
</form>
