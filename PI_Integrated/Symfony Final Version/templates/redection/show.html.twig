{% extends 'base.html.twig' %}

{% block title %}Redection{% endblock %}

{% block body %}
    <h1>Redection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Coder</th>
                <td>{{ redection.coder }}</td>
            </tr>
            <tr>
                <th>Valr</th>
                <td>{{ redection.valr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_redection_index') }}">back to list</a>

    <a href="{{ path('app_redection_edit', {'coder': redection.coder}) }}">edit</a>

    {{ include('redection/_delete_form.html.twig') }}
{% endblock %}
